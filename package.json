{
  "name": "web-gis-elements",
  "version": "0.1.0",
  "description": "Custom elements for Web GIS applications.",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run lint && npm run compile && npm run minify",
    "compile": "mkdir -p dist && babel src --out-dir src-babelfree --copy-files && browserify src-babelfree/index.js -o dist/bundle.js && rm -rf src-babelfree",
    "demo": "node demo/server.js 5000",
    "lint": "eslint \"src/**\"",
    "minify": "uglifyjs dist/bundle.js --output dist/bundle.min.js --source-map dist/bundle.min.js.map",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Xingchen Hong",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-eslint": "^6.1.2",
    "babel-plugin-transform-object-rest-spread": "^6.23.0",
    "babel-preset-es2015": "^6.24.0",
    "browserify": "^14.1.0",
    "eslint": "^3.18.0",
    "express": "^4.15.2",
    "lodash": "^4.17.4",
    "uglify-js": "^2.8.18"
  }
}
